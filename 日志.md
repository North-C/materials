开发计划：

```text
  1. 补最小运行参数：--cgroup-root=/sys/fs/cgroup/cpuset、--namespace-whitelist、--runtimeclass-whitelist、--dry-run。
  2. 补 cgroup path resolver，适配当前测试节点的 cgroup v1 + systemd slice 路径。
  3. 新增 Dockerfile.kata-cpuset-nri 和 DaemonSet manifest，挂载 /var/run/nri、/sys、/sys/fs/cgroup。
  4. 先用 dryRun=true 部署，确认插件能看到 kata Pod 和目标 sibling。
  5. 再关闭 dryRun，创建 1-2 个 2 CPU kata Pod，验证不会绑定到同一对 sibling。
```

注意：发布时，收紧 kata-cpuset-nri Deamonset 的权限。

补充用户使用文档，将远程的arm64环境作为测试条件，Daemonset 部署形式为默认选择，从编译镜像、设置Daemonset 参数。

---

Deepseek API-key：

sk-5b1c719d2d504b5eacffd01706ee668e

---

为了尽可能模拟真实的Agent使用场景，我给你提供 Deepseek api: sk-5b1c719d2d504b5eacffd01706ee668e，针对加入测试工具中的任务，逐个捕捉Agent的
  生成轨迹和分层方案
