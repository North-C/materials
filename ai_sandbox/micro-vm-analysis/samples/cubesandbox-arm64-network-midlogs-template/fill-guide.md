# Fill Guide

1. 先在 `commands.txt` 写清 quickcheck / check-procs / collect-logs 的命令。
2. 再在 `host.txt` 摘要 quickcheck 和进程检查结果。
3. 在 `logs.txt` 写清 collect-logs 产物路径和关键文件。
4. 最后在 `classification.md` 回对既有 `tap fd unavailable` 案例，并说明还缺什么。
