# classification

project = cubesandbox
line = guest-visible-restore
state = real

bucket =
success_baseline

confidence =
medium

status_note =
这是一份 guest-visible restore/update 成功基线样本，不是失败样本；适合作为后续失败样本的最小对照面。

baseline_kind =
report_derived_success_real
